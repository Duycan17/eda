[project]
name = "bts-reviews-eda-dashboard"
version = "0.1.0"
description = "Streamlit EDA dashboard for BTS review datasets"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.2.0",
  "plotly>=6.0.0",
  "streamlit>=1.44.0",
]

[tool.uv]
package = false
